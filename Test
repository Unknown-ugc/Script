local Players = game:GetService("Players")
local SoundService = game:GetService("SoundService")

local Player = Players.LocalPlayer

-- GUI
local Gui = Instance.new("ScreenGui")
Gui.Name = "StealAnEggAllScript"
Gui.ResetOnSpawn = false
Gui.Parent = Player:WaitForChild("PlayerGui")

-- COLORS
local Theme = Color3.fromRGB(35, 125, 255)
local Background = Color3.fromRGB(12, 18, 30)
local Card = Color3.fromRGB(18, 27, 43)
local Text = Color3.fromRGB(240, 245, 255)
local SubText = Color3.fromRGB(155, 170, 195)

-- MAIN
local Main = Instance.new("Frame")
Main.Size = UDim2.fromOffset(360, 410)
Main.Position = UDim2.new(0.5, -180, 0.5, -205)
Main.BackgroundColor3 = Background
Main.BorderSizePixel = 0
Main.Parent = Gui

local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 14)
Corner.Parent = Main

local Stroke = Instance.new("UIStroke")
Stroke.Color = Theme
Stroke.Thickness = 1.5
Stroke.Parent = Main

-- TOP
local Title = Instance.new("TextLabel")
Title.BackgroundTransparency = 1
Title.Position = UDim2.fromOffset(78, 12)
Title.Size = UDim2.fromOffset(240, 30)
Title.Font = Enum.Font.GothamBold
Title.Text = "Steal an Egg All Script"
Title.TextColor3 = Text
Title.TextSize = 16
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Main

local Subtitle = Instance.new("TextLabel")
Subtitle.BackgroundTransparency = 1
Subtitle.Position = UDim2.fromOffset(78, 36)
Subtitle.Size = UDim2.fromOffset(240, 18)
Subtitle.Font = Enum.Font.Gotham
Subtitle.Text = "All scripts in one place"
Subtitle.TextColor3 = SubText
Subtitle.TextSize = 10
Subtitle.TextXAlignment = Enum.TextXAlignment.Left
Subtitle.Parent = Main

-- NAV
local Nav = Instance.new("Frame")
Nav.Size = UDim2.fromOffset(62, 350)
Nav.Position = UDim2.fromOffset(8, 52)
Nav.BackgroundColor3 = Card
Nav.BorderSizePixel = 0
Nav.Parent = Main

local NavCorner = Instance.new("UICorner")
NavCorner.CornerRadius = UDim.new(0, 11)
NavCorner.Parent = Nav

local function NavButton(text, y)
	local Button = Instance.new("TextButton")
	Button.Size = UDim2.fromOffset(48, 48)
	Button.Position = UDim2.fromOffset(7, y)
	Button.BackgroundColor3 = Background
	Button.BorderSizePixel = 0
	Button.Font = Enum.Font.GothamBold
	Button.Text = text
	Button.TextColor3 = Text
	Button.TextSize = 9
	Button.Parent = Nav

	local C = Instance.new("UICorner")
	C.CornerRadius = UDim.new(0, 9)
	C.Parent = Button

	return Button
end

local HomeButton = NavButton("HOME", 8)
local ScriptButton = NavButton("SCRIPT", 62)
local SettingsButton = NavButton("SETTINGS", 116)

-- CONTENT
local Content = Instance.new("Frame")
Content.BackgroundTransparency = 1
Content.Position = UDim2.fromOffset(78, 60)
Content.Size = UDim2.fromOffset(272, 340)
Content.Parent = Main

-- HOME
local Home = Instance.new("Frame")
Home.BackgroundTransparency = 1
Home.Size = UDim2.fromScale(1, 1)
Home.Parent = Content

-- DISCORD
local Discord = Instance.new("ImageButton")
Discord.BackgroundColor3 = Card
Discord.Size = UDim2.fromOffset(42, 42)
Discord.Position = UDim2.fromOffset(2, 0)
Discord.Image = "https://www.roblox.com/asset-thumbnail/image?assetId=10367063084&width=420&height=420&format=png"
Discord.Parent = Home

local DCcorner = Instance.new("UICorner")
DCcorner.CornerRadius = UDim.new(1, 0)
DCcorner.Parent = Discord

-- PROFILE
local Avatar = Instance.new("ImageLabel")
Avatar.BackgroundTransparency = 1
Avatar.Size = UDim2.fromOffset(42, 42)
Avatar.Position = UDim2.new(1, -44, 0, 0)
Avatar.Parent = Home

local AvatarCorner = Instance.new("UICorner")
AvatarCorner.CornerRadius = UDim.new(1, 0)
AvatarCorner.Parent = Avatar

local Image = Players:GetUserThumbnailAsync(
	Player.UserId,
	Enum.ThumbnailType.HeadShot,
	Enum.ThumbnailSize.Size100x100
)

Avatar.Image = Image

local Username = Instance.new("TextLabel")
Username.BackgroundTransparency = 1
Username.Position = UDim2.new(1, -150, 0, 43)
Username.Size = UDim2.fromOffset(148, 18)
Username.Font = Enum.Font.GothamBold
Username.Text = "@" .. Player.Name
Username.TextColor3 = Text
Username.TextSize = 9
Username.TextXAlignment = Enum.TextXAlignment.Right
Username.Parent = Home

-- EXPLANATION CARD
local Info = Instance.new("ScrollingFrame")
Info.Position = UDim2.fromOffset(0, 62)
Info.Size = UDim2.fromOffset(272, 175)
Info.BackgroundColor3 = Card
Info.BorderSizePixel = 0
Info.ScrollBarThickness = 3
Info.ScrollBarImageColor3 = Theme
Info.CanvasSize = UDim2.fromOffset(0, 0)
Info.AutomaticCanvasSize = Enum.AutomaticSize.Y
Info.Parent = Home

local InfoCorner = Instance.new("UICorner")
InfoCorner.CornerRadius = UDim.new(0, 11)
InfoCorner.Parent = Info

local InfoText = Instance.new("TextLabel")
InfoText.BackgroundTransparency = 1
InfoText.Position = UDim2.fromOffset(12, 10)
InfoText.Size = UDim2.new(1, -24, 0, 0)
InfoText.AutomaticSize = Enum.AutomaticSize.Y
InfoText.Font = Enum.Font.Gotham
InfoText.Text = [[
Welcome to Steal an Egg All Script!

All available scripts are here in one simple hub. You can use the navigation on the left to switch between Home, Script, and Settings.

In the Script section, you can choose between KEY and KEYLESS. KEY contains scripts that may require a key, while KEYLESS contains scripts that don't require a key.

Just choose the script you want from the list and use the available option for it. Everything is organized into one place so you can easily find what you're looking for.

Settings lets you customize the hub, including the accent color and Song ID.

Most Used shows your top 3 most-used scripts during your current session.

Enjoy using Steal an Egg All Script!

Credit: Chris
]]
InfoText.TextColor3 = Text
InfoText.TextSize = 10
InfoText.TextWrapped = true
InfoText.TextYAlignment = Enum.TextYAlignment.Top
InfoText.Parent = Info

-- MOST USED
local MostTitle = Instance.new("TextLabel")
MostTitle.BackgroundTransparency = 1
MostTitle.Position = UDim2.fromOffset(0, 245)
MostTitle.Size = UDim2.fromOffset(272, 20)
MostTitle.Font = Enum.Font.GothamBold
MostTitle.Text = "MOST USED"
MostTitle.TextColor3 = Text
MostTitle.TextSize = 11
MostTitle.TextXAlignment = Enum.TextXAlignment.Left
MostTitle.Parent = Home

for i = 1, 3 do
	local Row = Instance.new("TextLabel")
	Row.BackgroundColor3 = Card
	Row.BorderSizePixel = 0
	Row.Position = UDim2.fromOffset(0, 245 + (i * 27))
	Row.Size = UDim2.fromOffset(272, 23)
	Row.Font = Enum.Font.Gotham
	Row.Text = i .. "   No scripts used yet"
	Row.TextColor3 = SubText
	Row.TextSize = 9
	Row.TextXAlignment = Enum.TextXAlignment.Left
	Row.Parent = Home

	local C = Instance.new("UICorner")
	C.CornerRadius = UDim.new(0, 7)
	C.Parent = Row
end

-- SCRIPT PAGE
local Scripts = Instance.new("Frame")
Scripts.BackgroundTransparency = 1
Scripts.Size = UDim2.fromScale(1, 1)
Scripts.Visible = false
Scripts.Parent = Content

local KeyTab = Instance.new("TextButton")
KeyTab.Size = UDim2.fromOffset(130, 32)
KeyTab.Position = UDim2.fromOffset(0, 0)
KeyTab.BackgroundColor3 = Theme
KeyTab.BorderSizePixel = 0
KeyTab.Font = Enum.Font.GothamBold
KeyTab.Text = "KEY"
KeyTab.TextColor3 = Text
KeyTab.TextSize = 10
KeyTab.Parent = Scripts

local KeyCorner = Instance.new("UICorner")
KeyCorner.CornerRadius = UDim.new(0, 8)
KeyCorner.Parent = KeyTab

local KeylessTab = Instance.new("TextButton")
KeylessTab.Size = UDim2.fromOffset(130, 32)
KeylessTab.Position = UDim2.fromOffset(140, 0)
KeylessTab.BackgroundColor3 = Card
KeylessTab.BorderSizePixel = 0
KeylessTab.Font = Enum.Font.GothamBold
KeylessTab.Text = "KEYLESS"
KeylessTab.TextColor3 = Text
KeylessTab.TextSize = 10
KeylessTab.Parent = Scripts

local KeylessCorner = Instance.new("UICorner")
KeylessCorner.CornerRadius = UDim.new(0, 8)
KeylessCorner.Parent = KeylessTab

local ScriptList = Instance.new("ScrollingFrame")
ScriptList.Position = UDim2.fromOffset(0, 42)
ScriptList.Size = UDim2.fromOffset(272, 285)
ScriptList.BackgroundTransparency = 1
ScriptList.BorderSizePixel = 0
ScriptList.ScrollBarThickness = 3
ScriptList.ScrollBarImageColor3 = Theme
ScriptList.CanvasSize = UDim2.fromOffset(0, 0)
ScriptList.AutomaticCanvasSize = Enum.AutomaticSize.Y
ScriptList.Parent = Scripts

local Layout = Instance.new("UIListLayout")
Layout.Padding = UDim.new(0, 7)
Layout.Parent = ScriptList

local function AddScript(name)
	local Button = Instance.new("TextButton")
	Button.Size = UDim2.new(1, -5, 0, 38)
	Button.BackgroundColor3 = Card
	Button.BorderSizePixel = 0
	Button.Font = Enum.Font.GothamMedium
	Button.Text = name
	Button.TextColor3 = Text
	Button.TextSize = 10
	Button.TextXAlignment = Enum.TextXAlignment.Left
	Button.Parent = ScriptList

	local Padding = Instance.new("UIPadding")
	Padding.PaddingLeft = UDim.new(0, 12)
	Padding.Parent = Button

	local C = Instance.new("UICorner")
	C.CornerRadius = UDim.new(0, 9)
	C.Parent = Button
end

local KeyScripts = {
	"Miranda hub",
	"Clover hub",
	"Ajjans hub",
	"Big Foot",
	"Speed Hub"
}

local KeylessScripts = {
	"Finding one player server",
	"Shader",
	"Lennon hub",
	"Fyy community",
	"Foxname",
	"Ouroboros",
	"Axonic",
	"bobloscript-v2",
	"Mcrzhub",
	"ub-hub"
}

for _, name in ipairs(KeyScripts) do
	AddScript(name)
end

-- SETTINGS
local Settings = Instance.new("ScrollingFrame")
Settings.BackgroundTransparency = 1
Settings.Size = UDim2.fromScale(1, 1)
Settings.Visible = false
Settings.ScrollBarThickness = 3
Settings.ScrollBarImageColor3 = Theme
Settings.CanvasSize = UDim2.fromOffset(0, 0)
Settings.AutomaticCanvasSize = Enum.AutomaticSize.Y
Settings.Parent = Content

local SettingsLayout = Instance.new("UIListLayout")
SettingsLayout.Padding = UDim.new(0, 8)
SettingsLayout.Parent = Settings

local function SettingTitle(text)
	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(1, -5, 0, 24)
	Label.BackgroundTransparency = 1
	Label.Font = Enum.Font.GothamBold
	Label.Text = text
	Label.TextColor3 = Text
	Label.TextSize = 11
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Settings
end

SettingTitle("ACCENT COLOR")

local Colors = {
	{"Blue", Color3.fromRGB(35,125,255)},
	{"Purple", Color3.fromRGB(145,80,255)},
	{"Cyan", Color3.fromRGB(30,210,220)},
	{"Green", Color3.fromRGB(50,200,100)},
	{"Red", Color3.fromRGB(235,65,75)},
	{"Orange", Color3.fromRGB(255,145,40)}
}

for _, data in ipairs(Colors) do
	local Button = Instance.new("TextButton")
	Button.Size = UDim2.new(1, -5, 0, 32)
	Button.BackgroundColor3 = Card
	Button.BorderSizePixel = 0
	Button.Font = Enum.Font.GothamMedium
	Button.Text = data[1]
	Button.TextColor3 = Text
	Button.TextSize = 10
	Button.Parent = Settings

	local C = Instance.new("UICorner")
	C.CornerRadius = UDim.new(0, 8)
	C.Parent = Button

	Button.Activated:Connect(function()
		Theme = data[2]
		Stroke.Color = Theme
		KeyTab.BackgroundColor3 = Theme
		ScriptList.ScrollBarImageColor3 = Theme
		Settings.ScrollBarImageColor3 = Theme
		Info.ScrollBarImageColor3 = Theme
	end)
end

SettingTitle("SONG ID")

local SongBox = Instance.new("TextBox")
SongBox.Size = UDim2.new(1, -5, 0, 36)
SongBox.BackgroundColor3 = Card
SongBox.BorderSizePixel = 0
SongBox.PlaceholderText = "Enter Roblox Song ID"
SongBox.PlaceholderColor3 = SubText
SongBox.Text = ""
SongBox.TextColor3 = Text
SongBox.Font = Enum.Font.Gotham
SongBox.TextSize = 10
SongBox.ClearTextOnFocus = false
SongBox.Parent = Settings

local SongCorner = Instance.new("UICorner")
SongCorner.CornerRadius = UDim.new(0, 8)
SongCorner.Parent = SongBox

local Play = Instance.new("TextButton")
Play.Size = UDim2.new(0.48, 0, 0, 32)
Play.BackgroundColor3 = Theme
Play.BorderSizePixel = 0
Play.Font = Enum.Font.GothamBold
Play.Text = "PLAY"
Play.TextColor3 = Text
Play.TextSize = 10
Play.Parent = Settings

local PlayCorner = Instance.new("UICorner")
PlayCorner.CornerRadius = UDim.new(0, 8)
PlayCorner.Parent = Play

local Stop = Instance.new("TextButton")
Stop.Size = UDim2.new(0.48, 0, 0, 32)
Stop.BackgroundColor3 = Card
Stop.BorderSizePixel = 0
Stop.Font = Enum.Font.GothamBold
Stop.Text = "STOP"
Stop.TextColor3 = Text
Stop.TextSize = 10
Stop.Parent = Settings

local StopCorner = Instance.new("UICorner")
StopCorner.CornerRadius = UDim.new(0, 8)
StopCorner.Parent = Stop

local Song = Instance.new("Sound")
Song.Name = "HubSong"
Song.Volume = 0.5
Song.Parent = SoundService

Play.Activated:Connect(function()
	local ID = tonumber(SongBox.Text)
	if ID then
		Song.SoundId = "rbxassetid://" .. ID
		Song:Play()
	end
end)

Stop.Activated:Connect(function()
	Song:Stop()
end)

-- TAB SWITCHING
KeyTab.Activated:Connect(function()
	KeyTab.BackgroundColor3 = Theme
	KeylessTab.BackgroundColor3 = Card

	for _, v in ipairs(ScriptList:GetChildren()) do
		if v:IsA("TextButton") then
			v:Destroy()
		end
	end

	for _, name in ipairs(KeyScripts) do
		AddScript(name)
	end
end)

KeylessTab.Activated:Connect(function()
	KeylessTab.BackgroundColor3 = Theme
	KeyTab.BackgroundColor3 = Card

	for _, v in ipairs(ScriptList:GetChildren()) do
		if v:IsA("TextButton") then
			v:Destroy()
		end
	end

	for _, name in ipairs(KeylessScripts) do
		AddScript(name)
	end
end)

-- PAGE SWITCH
local function ShowPage(page)
	Home.Visible = page == "Home"
	Scripts.Visible = page == "Script"
	Settings.Visible = page == "Settings"
end

HomeButton.Activated:Connect(function()
	ShowPage("Home")
end)

ScriptButton.Activated:Connect(function()
	ShowPage("Script")
end)

SettingsButton.Activated:Connect(function()
	ShowPage("Settings")
end)

-- DRAG
local UIS = game:GetService("UserInputService")

local dragging = false
local dragStart
local startPos

Title.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1
	or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = Main.Position

		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

UIS.InputChanged:Connect(function(input)
	if dragging and (
		input.UserInputType == Enum.UserInputType.MouseMovement
		or input.UserInputType == Enum.UserInputType.Touch
	) then
		local delta = input.Position - dragStart

		Main.Position = UDim2.new(
			startPos.X.Scale,
			startPos.X.Offset + delta.X,
			startPos.Y.Scale,
			startPos.Y.Offset + delta.Y
		)
	end
end)
