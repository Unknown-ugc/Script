local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")

local Player = Players.LocalPlayer

local Gui = Instance.new("ScreenGui")
Gui.Name = "StealAnEggAllScript"
Gui.ResetOnSpawn = false
Gui.Parent = Player:WaitForChild("PlayerGui")

local Theme = Color3.fromRGB(35,125,255)
local BG = Color3.fromRGB(10,16,28)
local Card = Color3.fromRGB(18,27,43)
local Text = Color3.fromRGB(240,245,255)
local Sub = Color3.fromRGB(150,165,190)

local Main = Instance.new("Frame")
Main.Size = UDim2.fromOffset(350,350)
Main.Position = UDim2.new(.5,-175,.5,-175)
Main.BackgroundColor3 = BG
Main.BorderSizePixel = 0
Main.Parent = Gui

Instance.new("UICorner",Main).CornerRadius = UDim.new(0,14)

local Stroke = Instance.new("UIStroke",Main)
Stroke.Color = Theme
Stroke.Thickness = 1.5

local Title = Instance.new("TextLabel",Main)
Title.BackgroundTransparency = 1
Title.Position = UDim2.fromOffset(76,10)
Title.Size = UDim2.fromOffset(210,25)
Title.Font = Enum.Font.GothamBold
Title.Text = "Steal an Egg All Script"
Title.TextColor3 = Text
Title.TextSize = 14
Title.TextXAlignment = Enum.TextXAlignment.Left

local SubTitle = Instance.new("TextLabel",Main)
SubTitle.BackgroundTransparency = 1
SubTitle.Position = UDim2.fromOffset(76,31)
SubTitle.Size = UDim2.fromOffset(210,18)
SubTitle.Font = Enum.Font.Gotham
SubTitle.Text = "All scripts in one place"
SubTitle.TextColor3 = Sub
SubTitle.TextSize = 9
SubTitle.TextXAlignment = Enum.TextXAlignment.Left

local Min = Instance.new("TextButton",Main)
Min.Size = UDim2.fromOffset(28,28)
Min.Position = UDim2.new(1,-65,0,9)
Min.BackgroundColor3 = Card
Min.BorderSizePixel = 0
Min.Text = "−"
Min.TextColor3 = Text
Min.Font = Enum.Font.GothamBold
Min.TextSize = 16
Instance.new("UICorner",Min).CornerRadius = UDim.new(0,8)

local Close = Instance.new("TextButton",Main)
Close.Size = UDim2.fromOffset(28,28)
Close.Position = UDim2.new(1,-33,0,9)
Close.BackgroundColor3 = Color3.fromRGB(45,28,38)
Close.BorderSizePixel = 0
Close.Text = "×"
Close.TextColor3 = Text
Close.Font = Enum.Font.GothamBold
Close.TextSize = 16
Instance.new("UICorner",Close).CornerRadius = UDim.new(0,8)

local Nav = Instance.new("Frame",Main)
Nav.Size = UDim2.fromOffset(58,300)
Nav.Position = UDim2.fromOffset(8,45)
Nav.BackgroundColor3 = Card
Nav.BorderSizePixel = 0
Instance.new("UICorner",Nav).CornerRadius = UDim.new(0,10)

local Content = Instance.new("Frame",Main)
Content.Size = UDim2.fromOffset(270,300)
Content.Position = UDim2.fromOffset(72,45)
Content.BackgroundTransparency = 1

local function navButton(text,y)
	local b = Instance.new("TextButton",Nav)
	b.Size = UDim2.fromOffset(46,42)
	b.Position = UDim2.fromOffset(6,y)
	b.BackgroundColor3 = BG
	b.BorderSizePixel = 0
	b.Text = text
	b.TextColor3 = Text
	b.Font = Enum.Font.GothamBold
	b.TextSize = 8
	Instance.new("UICorner",b).CornerRadius = UDim.new(0,8)
	return b
end

local HomeBtn = navButton("HOME",8)
local ScriptBtn = navButton("SCRIPT",56)
local SettingsBtn = navButton("SETTINGS",104)

local Home = Instance.new("Frame",Content)
Home.Size = UDim2.fromScale(1,1)
Home.BackgroundTransparency = 1

local Discord = Instance.new("ImageButton",Home)
Discord.Size = UDim2.fromOffset(40,40)
Discord.Position = UDim2.fromOffset(2,0)
Discord.BackgroundColor3 = Card
Discord.BorderSizePixel = 0
Discord.Image = "https://www.roblox.com/asset-thumbnail/image?assetId=10367063084&width=420&height=420&format=png"
Instance.new("UICorner",Discord).CornerRadius = UDim.new(1,0)

local Avatar = Instance.new("ImageLabel",Home)
Avatar.Size = UDim2.fromOffset(40,40)
Avatar.Position = UDim2.new(1,-42,0,0)
Avatar.BackgroundTransparency = 1
Instance.new("UICorner",Avatar).CornerRadius = UDim.new(1,0)

local Thumb = Players:GetUserThumbnailAsync(
	Player.UserId,
	Enum.ThumbnailType.HeadShot,
	Enum.ThumbnailSize.Size100x100
)

Avatar.Image = Thumb

local Username = Instance.new("TextLabel",Home)
Username.BackgroundTransparency = 1
Username.Position = UDim2.new(1,-145,0,40)
Username.Size = UDim2.fromOffset(143,16)
Username.Font = Enum.Font.GothamBold
Username.Text = "@"..Player.Name
Username.TextColor3 = Text
Username.TextSize = 8
Username.TextXAlignment = Enum.TextXAlignment.Right

local Info = Instance.new("ScrollingFrame",Home)
Info.Position = UDim2.fromOffset(0,52)
Info.Size = UDim2.fromOffset(270,145)
Info.BackgroundColor3 = Card
Info.BorderSizePixel = 0
Info.ScrollBarThickness = 3
Info.ScrollBarImageColor3 = Theme
Info.CanvasSize = UDim2.fromOffset(0,0)
Info.AutomaticCanvasSize = Enum.AutomaticSize.Y
Instance.new("UICorner",Info).CornerRadius = UDim.new(0,10)

local InfoText = Instance.new("TextLabel",Info)
InfoText.Position = UDim2.fromOffset(10,9)
InfoText.Size = UDim2.new(1,-20,0,0)
InfoText.AutomaticSize = Enum.AutomaticSize.Y
InfoText.BackgroundTransparency = 1
InfoText.Font = Enum.Font.Gotham
InfoText.Text = [[
Welcome to Steal an Egg All Script!

All available scripts are here in one simple hub. Everything is organized in one place so you can easily find the script you want.

Use the navigation on the left to switch between Home, Script, and Settings.

In Script, choose between KEY and KEYLESS. Pick the script you want from the list and press RUN.

KEY contains scripts that may require a key, while KEYLESS contains scripts that don't require a key.

Settings lets you customize the hub with different accent colors and a Roblox Song ID.

Most Used shows your top 3 scripts during your current session.

Enjoy using the hub!

Credit: Chris
]]
InfoText.TextColor3 = Text
InfoText.TextSize = 9
InfoText.TextWrapped = true
InfoText.TextYAlignment = Enum.TextYAlignment.Top

local Most = Instance.new("TextLabel",Home)
Most.BackgroundTransparency = 1
Most.Position = UDim2.fromOffset(0,205)
Most.Size = UDim2.fromOffset(270,18)
Most.Font = Enum.Font.GothamBold
Most.Text = "MOST USED"
Most.TextColor3 = Text
Most.TextSize = 10
Most.TextXAlignment = Enum.TextXAlignment.Left

for i = 1,3 do
	local row = Instance.new("TextLabel",Home)
	row.Size = UDim2.fromOffset(270,22)
	row.Position = UDim2.fromOffset(0,225+(i-26))
	row.BackgroundColor3 = Card
	row.BorderSizePixel = 0
	row.Font = Enum.Font.Gotham
	row.Text = i.."   No scripts used yet"
	row.TextColor3 = Sub
	row.TextSize = 8
	row.TextXAlignment = Enum.TextXAlignment.Left
	Instance.new("UICorner",row).CornerRadius = UDim.new(0,7)
end

local Scripts = Instance.new("Frame",Content)
Scripts.Size = UDim2.fromScale(1,1)
Scripts.BackgroundTransparency = 1
Scripts.Visible = false

local Key = Instance.new("TextButton",Scripts)
Key.Size = UDim2.fromOffset(130,30)
Key.BackgroundColor3 = Theme
Key.BorderSizePixel = 0
Key.Text = "KEY"
Key.TextColor3 = Text
Key.Font = Enum.Font.GothamBold
Key.TextSize = 9
Instance.new("UICorner",Key).CornerRadius = UDim.new(0,8)

local Keyless = Instance.new("TextButton",Scripts)
Keyless.Size = UDim2.fromOffset(130,30)
Keyless.Position = UDim2.fromOffset(140,0)
Keyless.BackgroundColor3 = Card
Keyless.BorderSizePixel = 0
Keyless.Text = "KEYLESS"
Keyless.TextColor3 = Text
Keyless.Font = Enum.Font.GothamBold
Keyless.TextSize = 9
Instance.new("UICorner",Keyless).CornerRadius = UDim.new(0,8)

local List = Instance.new("ScrollingFrame",Scripts)
List.Position = UDim2.fromOffset(0,38)
List.Size = UDim2.fromOffset(270,255)
List.BackgroundTransparency = 1
List.BorderSizePixel = 0
List.ScrollBarThickness = 3
List.ScrollBarImageColor3 = Theme
List.CanvasSize = UDim2.fromOffset(0,0)
List.AutomaticCanvasSize = Enum.AutomaticSize.Y

local Layout = Instance.new("UIListLayout",List)
Layout.Padding = UDim.new(0,6)

local KeyScripts = {
	"Miranda hub",
	"Clover hub",
	"Ajjans hub",
	"Big Foot",
	"Speed Hub"
}

local KeylessScripts = {
	"Finding one player server",
	"Shader",
	"Lennon hub",
	"Fyy community",
	"Foxname",
	"Ouroboros",
	"Axonic",
	"bobloscript-v2",
	"Mcrzhub",
	"ub-hub"
}

local function ClearList()
	for _,v in ipairs(List:GetChildren()) do
		if v:IsA("Frame") then
			v:Destroy()
		end
	end
end

local function AddScript(name)
	local Row = Instance.new("Frame",List)
	Row.Size = UDim2.new(1,-4,0,36)
	Row.BackgroundColor3 = Card
	Row.BorderSizePixel = 0
	Instance.new("UICorner",Row).CornerRadius = UDim.new(0,8)

	local Name = Instance.new("TextLabel",Row)
	Name.BackgroundTransparency = 1
	Name.Position = UDim2.fromOffset(10,0)
	Name.Size = UDim2.new(1,-75,1,0)
	Name.Font = Enum.Font.GothamMedium
	Name.Text = name
	Name.TextColor3 = Text
	Name.TextSize = 8
	Name.TextXAlignment = Enum.TextXAlignment.Left

	local Run = Instance.new("TextButton",Row)
	Run.Size = UDim2.fromOffset(55,25)
	Run.Position = UDim2.new(1,-62,.5,-12)
	Run.BackgroundColor3 = Theme
	Run.BorderSizePixel = 0
	Run.Text = "RUN"
	Run.TextColor3 = Text
	Run.Font = Enum.Font.GothamBold
	Run.TextSize = 8
	Instance.new("UICorner",Run).CornerRadius = UDim.new(0,7)

	Run.Activated:Connect(function()
		print("TEST UI: "..name)
	end)
end

local function ShowScripts(list)
	ClearList()
	for _,name in ipairs(list) do
		AddScript(name)
	end
end

ShowScripts(KeyScripts)

Key.Activated:Connect(function()
	Key.BackgroundColor3 = Theme
	Keyless.BackgroundColor3 = Card
	ShowScripts(KeyScripts)
end)

Keyless.Activated:Connect(function()
	Keyless.BackgroundColor3 = Theme
	Key.BackgroundColor3 = Card
	ShowScripts(KeylessScripts)
end)

local Settings = Instance.new("ScrollingFrame",Content)
Settings.Size = UDim2.fromScale(1,1)
Settings.BackgroundTransparency = 1
Settings.Visible = false
Settings.ScrollBarThickness = 3
Settings.ScrollBarImageColor3 = Theme
Settings.CanvasSize = UDim2.fromOffset(0,0)
Settings.AutomaticCanvasSize = Enum.AutomaticSize.Y

local SetLayout = Instance.new("UIListLayout",Settings)
SetLayout.Padding = UDim.new(0,7)

local SetTitle = Instance.new("TextLabel",Settings)
SetTitle.Size = UDim2.new(1,0,0,22)
SetTitle.BackgroundTransparency = 1
SetTitle.Text = "ACCENT COLOR"
SetTitle.TextColor3 = Text
SetTitle.Font = Enum.Font.GothamBold
SetTitle.TextSize = 10
SetTitle.TextXAlignment = Enum.TextXAlignment.Left

local Colors = {
	{"Blue",Color3.fromRGB(35,125,255)},
	{"Purple",Color3.fromRGB(145,80,255)},
	{"Cyan",Color3.fromRGB(30,210,220)},
	{"Green",Color3.fromRGB(50,200,100)},
	{"Red",Color3.fromRGB(235,65,75)},
	{"Orange",Color3.fromRGB(255,145,40)}
}

for _,data in ipairs(Colors) do
	local B = Instance.new("TextButton",Settings)
	B.Size = UDim2.new(1,0,0,30)
	B.BackgroundColor3 = Card
	B.BorderSizePixel = 0
	B.Text = data[1]
	B.TextColor3 = Text
	B.Font = Enum.Font.Gotham
	B.TextSize = 9
	Instance.new("UICorner",B).CornerRadius = UDim.new(0,8)

	B.Activated:Connect(function()
		Theme = data[2]
		Stroke.Color = Theme
		Key.BackgroundColor3 = Theme
		List.ScrollBarImageColor3 = Theme
		Info.ScrollBarImageColor3 = Theme
		Settings.ScrollBarImageColor3 = Theme
	end)
end

local SongTitle = Instance.new("TextLabel",Settings)
SongTitle.Size = UDim2.new(1,0,0,22)
SongTitle.BackgroundTransparency = 1
SongTitle.Text = "SONG ID"
SongTitle.TextColor3 = Text
SongTitle.Font = Enum.Font.GothamBold
SongTitle.TextSize = 10
SongTitle.TextXAlignment = Enum.TextXAlignment.Left

local SongBox = Instance.new("TextBox",Settings)
SongBox.Size = UDim2.new(1,0,0,34)
SongBox.BackgroundColor3 = Card
SongBox.BorderSizePixel = 0
SongBox.PlaceholderText = "Enter Roblox Song ID"
SongBox.PlaceholderColor3 = Sub
SongBox.TextColor3 = Text
SongBox.Font = Enum.Font.Gotham
SongBox.TextSize = 9
SongBox.ClearTextOnFocus = false
Instance.new("UICorner",SongBox).CornerRadius = UDim.new(0,8)

local Play = Instance.new("TextButton",Settings)
Play.Size = UDim2.new(1,0,0,30)
Play.BackgroundColor3 = Theme
Play.BorderSizePixel = 0
Play.Text = "PLAY"
Play.TextColor3 = Text
Play.Font = Enum.Font.GothamBold
Play.TextSize = 9
Instance.new("UICorner",Play).CornerRadius = UDim.new(0,8)

local Stop = Instance.new("TextButton",Settings)
Stop.Size = UDim2.new(1,0,0,30)
Stop.BackgroundColor3 = Card
Stop.BorderSizePixel = 0
Stop.Text = "STOP"
Stop.TextColor3 = Text
Stop.Font = Enum.Font.GothamBold
Stop.TextSize = 9
Instance.new("UICorner",Stop).CornerRadius = UDim.new(0,8)

local function Page(page)
	Home.Visible = page == "Home"
	Scripts.Visible = page == "Script"
	Settings.Visible = page == "Settings"
end

HomeBtn.Activated:Connect(function()
	Page("Home")
end)

ScriptBtn.Activated:Connect(function()
	Page("Script")
end)

SettingsBtn.Activated:Connect(function()
	Page("Settings")
end)

-- MINIMIZED CIRCLE
local Mini = Instance.new("ImageButton")
Mini.Size = UDim2.fromOffset(50,50)
Mini.Position = UDim2.new(0,20,.5,-25)
Mini.BackgroundColor3 = BG
Mini.BorderSizePixel = 0
Mini.Image = "https://www.roblox.com/asset-thumbnail/image?assetId=85422381974829&width=420&height=420&format=png"
Mini.Visible = false
Mini.Parent = Gui
Instance.new("UICorner",Mini).CornerRadius = UDim.new(1,0)

local MiniStroke = Instance.new("UIStroke",Mini)
MiniStroke.Color = Theme
MiniStroke.Thickness = 2

Min.Activated:Connect(function()
	Main.Visible = false
	Mini.Visible = true
end)

Mini.Activated:Connect(function()
	Mini.Visible = false
	Main.Visible = true
end)

Close.Activated:Connect(function()
	Gui:Destroy()
end)

-- DRAG
local dragging = false
local dragStart
local startPos

Title.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1
	or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = Main.Position
	end
end)

UIS.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1
	or input.UserInputType == Enum.UserInputType.Touch then
		dragging = false
	end
end)

UIS.InputChanged:Connect(function(input)
	if dragging and (
		input.UserInputType == Enum.UserInputType.MouseMovement
		or input.UserInputType == Enum.UserInputType.Touch
	) then
		local delta = input.Position - dragStart
		Main.Position = UDim2.new(
			startPos.X.Scale,
			startPos.X.Offset + delta.X,
			startPos.Y.Scale,
			startPos.Y.Offset + delta.Y
		)
	end
end)
