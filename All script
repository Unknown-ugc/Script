local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")

local player = Players.LocalPlayer

local gui = Instance.new("ScreenGui")
gui.Name = "StealAnEggAllScript"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

-- Loading screen
local loading = Instance.new("Frame")
loading.Size = UDim2.fromOffset(270, 270)
loading.Position = UDim2.new(0.5, -135, 0.5, -135)
loading.BackgroundColor3 = Color3.fromRGB(20, 30, 45)
loading.BorderSizePixel = 0
loading.Parent = gui

local loadCorner = Instance.new("UICorner")
loadCorner.CornerRadius = UDim.new(0, 16)
loadCorner.Parent = loading

local loadTitle = Instance.new("TextLabel")
loadTitle.BackgroundTransparency = 1
loadTitle.Size = UDim2.new(1, 0, 0, 45)
loadTitle.Position = UDim2.fromOffset(0, 55)
loadTitle.Text = "SERVER HUB"
loadTitle.TextColor3 = Color3.new(1, 1, 1)
loadTitle.Font = Enum.Font.GothamBold
loadTitle.TextSize = 25
loadTitle.Parent = loading

local percent = Instance.new("TextLabel")
percent.BackgroundTransparency = 1
percent.Size = UDim2.new(1, 0, 0, 40)
percent.Position = UDim2.fromOffset(0, 105)
percent.Text = "0%"
percent.TextColor3 = Color3.new(1, 1, 1)
percent.Font = Enum.Font.GothamBold
percent.TextSize = 22
percent.Parent = loading

local barBack = Instance.new("Frame")
barBack.Size = UDim2.fromOffset(210, 8)
barBack.Position = UDim2.fromOffset(30, 160)
barBack.BackgroundColor3 = Color3.fromRGB(45, 55, 70)
barBack.BorderSizePixel = 0
barBack.Parent = loading

local barBackCorner = Instance.new("UICorner")
barBackCorner.CornerRadius = UDim.new(1, 0)
barBackCorner.Parent = barBack

local bar = Instance.new("Frame")
bar.Size = UDim2.new(0, 0, 1, 0)
bar.BackgroundColor3 = Color3.fromRGB(40, 140, 255)
bar.BorderSizePixel = 0
bar.Parent = barBack

local barCorner = Instance.new("UICorner")
barCorner.CornerRadius = UDim.new(1, 0)
barCorner.Parent = bar

-- Main GUI
local main = Instance.new("Frame")
main.Name = "Main"
main.Size = UDim2.fromOffset(270, 340)
main.Position = UDim2.new(0.5, -135, 0.5, -170)
main.BackgroundColor3 = Color3.fromRGB(18, 27, 40)
main.BorderSizePixel = 0
main.Visible = false
main.Parent = gui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 14)
mainCorner.Parent = main

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(45, 150, 255)
stroke.Thickness = 1
stroke.Parent = main

-- Drag area
local drag = Instance.new("TextButton")
drag.Size = UDim2.new(1, -70, 0, 38)
drag.BackgroundTransparency = 1
drag.Text = ""
drag.Parent = main

-- Title
local title = Instance.new("TextLabel")
title.BackgroundTransparency = 1
title.Size = UDim2.new(1, -75, 0, 38)
title.Position = UDim2.fromOffset(10, 0)
title.Text = "Steal an Egg All Script"
title.TextColor3 = Color3.new(1, 1, 1)
title.Font = Enum.Font.GothamBold
title.TextSize = 14
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = main

-- Minimize
local minimize = Instance.new("TextButton")
minimize.Size = UDim2.fromOffset(30, 30)
minimize.Position = UDim2.new(1, -65, 0, 4)
minimize.BackgroundTransparency = 1
minimize.Text = "−"
minimize.TextColor3 = Color3.new(1, 1, 1)
minimize.Font = Enum.Font.GothamBold
minimize.TextSize = 22
minimize.Parent = main

-- Close
local close = Instance.new("TextButton")
close.Size = UDim2.fromOffset(30, 30)
close.Position = UDim2.new(1, -32, 0, 4)
close.BackgroundTransparency = 1
close.Text = "X"
close.TextColor3 = Color3.new(1, 1, 1)
close.Font = Enum.Font.GothamBold
close.TextSize = 15
close.Parent = main

-- KEY tab
local keyTab = Instance.new("TextButton")
keyTab.Size = UDim2.fromOffset(120, 28)
keyTab.Position = UDim2.fromOffset(8, 40)
keyTab.BackgroundColor3 = Color3.fromRGB(29, 39, 55)
keyTab.BorderSizePixel = 0
keyTab.Text = "KEY"
keyTab.TextColor3 = Color3.new(1, 1, 1)
keyTab.Font = Enum.Font.GothamBold
keyTab.TextSize = 11
keyTab.Parent = main

-- KEYLESS tab
local keylessTab = Instance.new("TextButton")
keylessTab.Size = UDim2.fromOffset(120, 28)
keylessTab.Position = UDim2.fromOffset(142, 40)
keylessTab.BackgroundColor3 = Color3.fromRGB(35, 110, 190)
keylessTab.BorderSizePixel = 0
keylessTab.Text = "KEYLESS"
keylessTab.TextColor3 = Color3.new(1, 1, 1)
keylessTab.Font = Enum.Font.GothamBold
keylessTab.TextSize = 11
keylessTab.Parent = main

for _, tab in ipairs({keyTab, keylessTab}) do
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 7)
    corner.Parent = tab
end

-- List
local list = Instance.new("ScrollingFrame")
list.Name = "ScriptList"
list.Size = UDim2.fromOffset(254, 185)
list.Position = UDim2.fromOffset(8, 74)
list.BackgroundTransparency = 1
list.BorderSizePixel = 0
list.ScrollBarThickness = 3
list.CanvasSize = UDim2.new(0, 0, 0, 0)
list.Parent = main

local layout = Instance.new("UIListLayout")
layout.Padding = UDim.new(0, 5)
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
layout.Parent = list

layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    list.CanvasSize = UDim2.new(
        0,
        0,
        0,
        layout.AbsoluteContentSize.Y + 8
    )
end)

-- Button function
local function addName(name, runFunction)
    local button = Instance.new("TextButton")

    button.Name = name
    button.Size = UDim2.new(1, -4, 0, 34)
    button.BackgroundColor3 = Color3.fromRGB(29, 39, 55)
    button.BorderSizePixel = 0
    button.Text = name
    button.TextColor3 = Color3.new(1, 1, 1)
    button.TextSize = 11
    button.Font = Enum.Font.GothamBold
    button.AutoButtonColor = true
    button.Parent = list

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 9)
    corner.Parent = button

    button.Activated:Connect(function()
        task.spawn(function()
            pcall(runFunction)
        end)
    end)
end

-- KEY BUTTONS
local keyNames = {
    "Miranda hub",
    "Clover hub",
    "Ajjans hub",
    "Big Foot",
    "Speed Hub"
}

-- KEYLESS BUTTONS
local keylessNames = {
    "Finding one player server",
    "Lennon hub",
    "Fyy community",
    "Foxname",
    "Ouroboros",
    "Axonic",
    "bobloscript-v2",
    "Mcrzhub",
    "ub-hub"
}

local function clearList()
    for _, child in ipairs(list:GetChildren()) do
        if child:IsA("TextButton") then
            child:Destroy()
        end
    end
end

-- KEY TAB
local function showKey()
    clearList()

    addName("Miranda hub", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/miirandahub/loader/refs/heads/main/stealaegg"))()

end)

    addName("Clover hub", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Ryuun0x/Clover/refs/heads/main/main.lua"))()

end)

    addName("Ajjans hub", function()
        loadstring(game:HttpGet("https://api.luarmor.net/files/v4/loaders/359e97f8618e9008afe5f496184ebb7c.lua"))()

end)

    addName("Big Foot", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/hanniii1/Loader/refs/heads/main/BFLoader.lua"))()

end)

    addName("Speed Hub", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/AhmadV99/Speed-Hub-X/main/Speed%20Hub%20X.lua", true))()

end)

    -- Highlight KEY
    keyTab.BackgroundColor3 = Color3.fromRGB(35, 110, 190)
    keylessTab.BackgroundColor3 = Color3.fromRGB(29, 39, 55)
end

-- KEYLESS TAB
local function showKeyless()
    clearList()

    addName("Finding one player server", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/showscript-hub/Script/refs/heads/main/Hop%20Server%20list"))()
 
end)

    addName("Lennon hub", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/lennonxscripts/lennonhubv2/refs/heads/main/stealaneggv2"))()

end)

    addName("Fyy community", function()
        loadstring(game:HttpGet("https://FyyCommunity.my.id"))()

end)

    addName("Foxname", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/caomod2077/Script/refs/heads/main/Fn-stealanegg.lua"))()

end)

    addName("Ouroboros", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/joustingmatch/Ouroboros/main/loader.lua"))(

end)

    addName("Axonic", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Kenniel123/Steal-A-Egg/refs/heads/main/Steal%20A%20Egg"))()
 
end)

    addName("bobloscript-v2", function()
        loadstring(game:HttpGet("https://github.com/bobloscript/scripts/raw/refs/heads/main/opensource_egg", true))()
   

end)

    addName("Mcrzhub", function()
        loadstring(game:HttpGet("https://flowauth.net/v1/loaders/3c4e87ed34813171b0f8d53a108a7d88.lua"))() 
  
end)

    addName("ub-hub", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/TeamUBHub/UBLoader/refs/heads/main/index/Key.lua"))()
 
end)

    -- Highlight KEYLESS
    keyTab.BackgroundColor3 = Color3.fromRGB(29, 39, 55)
    keylessTab.BackgroundColor3 = Color3.fromRGB(35, 110, 190)
end

-- Tab clicks
keyTab.Activated:Connect(showKey)
keylessTab.Activated:Connect(showKeyless)

-- Credit
local credit = Instance.new("TextLabel")
credit.BackgroundTransparency = 1
credit.Position = UDim2.fromOffset(10, 267)
credit.Size = UDim2.fromOffset(120, 18)
credit.Text = "Credit: Chris"
credit.TextColor3 = Color3.fromRGB(190, 200, 215)
credit.Font = Enum.Font.Gotham
credit.TextSize = 10
credit.TextXAlignment = Enum.TextXAlignment.Left
credit.Parent = main

-- Discord
local discord = Instance.new("TextButton")
discord.BackgroundTransparency = 1
discord.Position = UDim2.fromOffset(10, 287)
discord.Size = UDim2.fromOffset(120, 18)
discord.Text = "Discord Server"
discord.TextColor3 = Color3.fromRGB(80, 170, 255)
discord.Font = Enum.Font.GothamBold
discord.TextSize = 10
discord.TextXAlignment = Enum.TextXAlignment.Left
discord.Parent = main

discord.Activated:Connect(function()
    if setclipboard then
        setclipboard("https://discord.gg/FJK6QzNnf7")
    end
end)

-- Avatar
local avatar = Instance.new("ImageLabel")
avatar.BackgroundTransparency = 1
avatar.Size = UDim2.fromOffset(42, 42)
avatar.Position = UDim2.new(1, -54, 1, -65)
avatar.Parent = main

local avatarCorner = Instance.new("UICorner")
avatarCorner.CornerRadius = UDim.new(1, 0)
avatarCorner.Parent = avatar

local success, content = pcall(function()
    return Players:GetUserThumbnailAsync(
        player.UserId,
        Enum.ThumbnailType.HeadShot,
        Enum.ThumbnailSize.Size150x150
    )
end)

if success then
    avatar.Image = content
end

-- Username
local username = Instance.new("TextLabel")
username.BackgroundTransparency = 1
username.Position = UDim2.new(1, -80, 1, -21)
username.Size = UDim2.fromOffset(70, 16)
username.Text = "@" .. player.Name
username.TextColor3 = Color3.new(1, 1, 1)
username.Font = Enum.Font.Gotham
username.TextSize = 9
username.TextXAlignment = Enum.TextXAlignment.Center
username.Parent = main

-- Minimized button
local mini = Instance.new("ImageButton")
mini.Size = UDim2.fromOffset(52, 52)
mini.Position = UDim2.new(0.5, -26, 0, 8)
mini.BackgroundColor3 = Color3.fromRGB(18, 27, 40)
mini.BorderSizePixel = 0
mini.Visible = false
mini.Parent = gui

local miniCorner = Instance.new("UICorner")
miniCorner.CornerRadius = UDim.new(1, 0)
miniCorner.Parent = mini

local miniStroke = Instance.new("UIStroke")
miniStroke.Color = Color3.fromRGB(45, 150, 255)
miniStroke.Thickness = 2
miniStroke.Parent = mini

mini.Image = "https://www.roblox.com/asset-thumbnail/image?assetId=85422381974829&width=420&height=420&format=png"

-- Minimize
minimize.Activated:Connect(function()
    main.Visible = false
    mini.Visible = true
end)

-- Reopen
mini.Activated:Connect(function()
    mini.Visible = false
    main.Visible = true
end)

-- Close
close.Activated:Connect(function()
    gui:Destroy()
end)

-- Mobile dragging
local dragging = false
local dragStart
local startPos

drag.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then

        dragging = true
        dragStart = input.Position
        startPos = main.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

UIS.InputChanged:Connect(function(input)
    if dragging and (
        input.UserInputType == Enum.UserInputType.MouseMovement
        or input.UserInputType == Enum.UserInputType.Touch
    ) then

        local delta = input.Position - dragStart

        main.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

-- Loading
task.spawn(function()
    for i = 0, 100 do
        percent.Text = i .. "%"
        bar.Size = UDim2.new(i / 100, 0, 1, 0)
        task.wait(0.03)
    end

    percent.Text = "100%"
    bar.Size = UDim2.new(1, 0, 1, 0)

    task.wait(0.2)

    loading:Destroy()
    main.Visible = true

    -- Start on KEYLESS
    showKeyless()
end)
